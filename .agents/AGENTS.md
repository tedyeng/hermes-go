# Hermes Go Workspace Rules

- **Documentation Updates**: Whenever a new feature or command is added, the corresponding module-level `README.md`, the root `README.md`, and project development documentation inside `docs/` (such as implementation plans, task checklists, and walkthroughs) MUST be kept fully updated to reflect all functional and CLI syntax changes.
